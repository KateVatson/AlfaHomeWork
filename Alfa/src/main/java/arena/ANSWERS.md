1. Это общий счётчик для всех героев. Неважно, какого именно героя я создала, количество созданных героев должно храниться в одном месте и быть одинаковым для всей игры.

2. Это характеристики конкретного героя, поэтому у каждого объекта должны быть свои значения этих полей. Если сделать эти поля статическими, то изменение имени у одного героя изменит его сразу у всех остальных

3. Потому что метод статический и он принадлежит самому классу Hero и работает с общими данными класса, поэтому для его вызова не нужно создавать героя

4. Нет, потому что статический метод не знает с каким именно объектом он работает. Поле name существует внутри конкретного героя, а не внутри класса и Java не понимает, имя какого героя нужно взять

5. ОТ final классов нельзя наследоваться. А у нас Knight, Archer и Mage наследуются от Hero

6. Потому что в Java работает полиморфизм. Переменная может иметь тип Hero, но внутри неё лежит объект Knight, Archer или Mage. Во время выполнения программа смотрит на реальный объект и вызывает его версию метода attack()

7. Потому что final запрещает заменить объект на другой, но не запрещает менять его данные и так сам объект остаётся тем же, просто его поля могут изменяться